#include <stdlib.h>
#include <string.h>

//encodes a frame in differential manchester
char* differential_manchester_encoder(char* frame);

//encodes a frame in manchester
char* manchester_encoder(char* frame);